import { Card, CardTitle } from "@/components/ui/card";

const Analytics = () => {
    return (
        <Card className="flex-1 p-4 max-w-[35%]">
            <CardTitle>Analitycs</CardTitle>
        </Card>
    );
};

export default Analytics;
