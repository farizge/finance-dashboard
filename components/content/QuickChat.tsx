import React from "react";
import { Card, CardTitle } from "@/components/ui/card";

const QuickChat = () => {
    return (
        <Card className="w-[50%]">
            <CardTitle>Ester Howard</CardTitle>
        </Card>
    );
};

export default QuickChat;
